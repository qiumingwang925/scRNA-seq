# Keep the build context small. No data is bundled into the image — it arrives
# at runtime through the datasets/ bind mount.
.git
.claude
manuscript_R
datasets
test-data
**/.Rproj.user
**/.Rhistory
**/.RData
**/omnipathr-log
**/.DS_Store
